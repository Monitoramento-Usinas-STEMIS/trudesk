<div class="small-12 large-10 columns nopadding errorTypesContent" ng-controller="settingsCtrl">
    <div class="page-title noshadow" style="padding-left: 25px;">
        <p class="left">{{title}}</p>
        <div class="uk-float-right">
            <div class="pagination uk-float-left">
                <ul class="button-group uk-float-left">
                    <li class="pagination relative">
                        <div class="right" data-uk-dropdown="{pos: 'bottom-right', mode: 'click'}" aria-haspopup="true" aria-expanded="false">
                            <a href="#" class="no-ajaxy md-btn md-btn-success" style="margin-top: 15px;" ng-click="showCreateErrorTypeWindow($event)">Create</a>
                            <!--<a href="#" class="btn no-ajaxy" data-notifications="ticket-actions" style="border-radius: 3px;"><i class="fa fa-large fa-tasks"></i></a>-->
                            <!--<div class="uk-dropdown uk-dropdown-xsmall uk-dropdown-bottom">-->
                                <!--<ul class="uk-nav">-->
                                    <!--<li>-->
                                        <!--<a href="#" role="button" class="no-ajaxy" ng-click="showCreateErrorTypeWindow($event)">Create</a>-->
                                    <!--</li>-->
                                <!--</ul>-->
                            <!--</div>-->
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <div class="full-height page-content no-border-top" style="position: relative;">
        <div class="errorTypesList full-height">
            <table id="errorTypesTable" class="pDataTable stripe">
                <thead>
                <tr>
                    <th></th>
                    <th>Name</th>
                    <!--<th style="text-align: right; padding-right: 25px;">Ticket Count</th>-->
                </tr>
                </thead>
                <tbody>
                {{#each data.errorTypes}}
                    <tr ng-click="editErrorType($event)" data-errorTypeOId="{{errorType._id}}">
                        <td>
                            <input id="c_{{errorType._id}}" type="checkbox" class="poloCheckbox"/>
                            <label for="c_{{errorType._id}}"></label>
                        </td>
                        <td>{{errorType.name}}</td>
                        <!--<td style="text-align: right; padding-right: 25px;">{{count}}</td>-->
                    </tr>
                {{/each}}
                </tbody>
            </table>
        </div>
    </div>
    {{> createErrorTypeWindow}}
</div>

{{#contentFor 'js-plugins'}}

{{/contentFor}}